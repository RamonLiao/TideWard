tasks/progress.md
tasks/lessons.md

# Claude local config (contains permission allowlist incl. testnet key)
.claude/

# Secrets / keys
.env
.env.*
!.env.example
*.pem
*.key

# Move build artifacts
move/**/build/

# Brainstorm visual companion sessions
.superpowers/

# app build & generated env
app/dist/
app/.env.local

# playwright + verification artifacts
.playwright-mcp/
riskguard-console-live.png

# keep dummy test env (vitest needs it; overrides .env.* rule)
!app/.env.test
